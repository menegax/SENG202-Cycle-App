package seng202.team7;

public class StationTest {
    //really no need for tests as all functions in here are very basic,  e.g. no dependencies on other classes.
    //also there are data validity checks when this object is created anyway, with exception catching
}
